const product_mock = require('./product_mock');
const order_mock = require('./order_mock');
const category_mock = require('./category_mock');
const customer_mock = require('./customer_mock');

const test = {
  category_mock,
  customer_mock,
  product_mock,
  order_mock
}

module.exports = test;
